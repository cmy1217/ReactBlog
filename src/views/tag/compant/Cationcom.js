import React, { Component } from 'react'
import Contain from './Contain'
export default class Cationcom extends Component {
    render() {
        return (
           <Contain id={this.props.id}>

           </Contain>
        )
    }
}
